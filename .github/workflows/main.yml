name: TDS GA 2 Q5 Action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Step for 23f2002096@ds.study.iitm.ac.in
        run: echo "Hello from the requested step!"
